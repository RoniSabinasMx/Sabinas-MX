# ============================================================
# SABINAS SPA — .gitignore
# ============================================================

# 🔐 NEVER commit real config (contains API keys)
config/config.js

# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/settings.json
.idea/

# Node (if added later)
node_modules/
